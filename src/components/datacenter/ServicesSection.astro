---
import IconifyIcon from "@/components/IconifyIcon.vue";
import { createTranslator } from "@/i18n/utils";

const t = createTranslator("th");

// Services data
const serviceCards = [
  {
    title: "Cloud Server",
    description:
      "บริการเครื่องแม่ข่ายเสมือนทั้งออนพรีมิสและออนคลาวด์ | Veeam Backup | DR-Site",
    icon: "ic:baseline-cloud",
  },
  {
    title: "Google Workspace",
    description: "บริการแอคเค้าท์จดหมายอิเล็กทรอนิกส์ ไดรฟ์ และอื่นๆ",
    icon: "ri:google-fill",
  },
  {
    title: "Web Hosting",
    description:
      "บริการพื้นที่รับฝากเว็บไซต์สำหรับหน่วยงาน/คณะ บนเว็บเซิร์ฟเวอร์ IIS Apache และ NginX",
    icon: "clarity:host-group-solid",
  },
  {
    title: "Web Security",
    description: "บริการ TLS Certificate ให้กับเว็บไซต์ภายใต้โดเมน RSU",
    icon: "material-symbols:shield",
  },
];
---

<section id="services" class="container mx-auto px-2 sm:px-4 py-16">
  <div class="text-center mb-12">
    <h2 class="text-3xl font-bold mb-4">{t("datacenter.services.title")}</h2>
    <p class="text-xl text-base-content/60">
      {t("datacenter.services.subtitle")}
    </p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
    {
      serviceCards.map((service) => (
        <div class="bg-base-200 rounded-lg p-6 h-64 flex flex-col shadow-lg hover:shadow-xl transition-shadow">
          <div class="flex justify-center mb-4">
            <IconifyIcon
              client:visible
              icon={service.icon}
              className="w-10 h-10 text-base-content"
            />
          </div>
          <h3 class="text-xl font-bold mb-4">{service.title}</h3>
          <p class="text-base-content/80 flex-grow text-sm leading-relaxed">
            {service.description}
          </p>
        </div>
      ))
    }
  </div>
</section>
